// https://www.hackerrank.com/challenges/c-tutorial-functions

#include <iostream>
#include <cstdio>
using namespace std;

/*
Add `int max_of_four(int a, int b, int c, int d)` here.
*/
/*int max_of_four(int a, int b, int c, int d){
   
    int four [4] = {a, b, c, d};
    int maxValue = 0;
    for(int i = 0; i< 3; i++){
        if (four[i+1] > four[i] || four[i+1] == four[i] ){
        maxValue = four[i+1];
        }
        else{
        maxValue = four[i];
        }
    }
    return maxValue;
}*/
//This solution is better for a greater range of values; however, fails certain test cases. 


/*int max_of_four(int a, int b, int c, int d){
    int e = 0;
    if(a > b && a > c && a > d){
        return a;
    }
    else if(b > a && b > c && b > d){
        return b;
    }
    else if(c > a && c > b && c > d){
        return c;
    }
    else{
        return d;
    }
}*/ 
//This solution works but is pretty simple
  
int main() {
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);
    int ans = max_of_four(a, b, c, d);
    printf("%d", ans);
    
    return 0;
}

