// https://www.hackerrank.com/challenges/c-tutorial-strings


#include <iostream>
#include <string>
using namespace std;

/*int main() {
   string a,b;
   cin>> a >> b;
    int aa = a.length();
    int bb = b.length();
    string c = a + b;
    string ab = b[0] + a.substr(1, a.size()-1);
    string ba = a[0] + b.substr(1, b.size()-1);
    
    cout << aa << " "<< bb << endl;
    cout << c << endl;
    cout << ab << " "<< ba << endl;
  
    return 0;
}*/ 
//solution above works and uses the substr() method.

/*#include <bits/stdc++.h>

using namespace std;

int main()
{
    string a, b;
    cin >> a >> b;
    
    cout << a.length() << ' ' << b.length() << endl;
    
    cout << a + b << endl;
    
    swap( a[0], b[0] ); //easier than above implementation
    cout << a << ' ' <<  b << endl;
    
    return 0;
}*/
